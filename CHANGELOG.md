# Changelog

本项目按 Keep a Changelog 的结构记录重要变化。

## [Unreleased]

### Added

- 严格的五场景内容注册表和跨文档引用校验。
- 包含 schema、game ID 和 revision 的权威 `GameState`。
- 确定性探索、对话效果、关键选择、场景跳转和四结局服务。
- DeepSeek 结构化对白适配器：超时、有限重试、熔断和角色化降级。
- Argon2id 密码、HttpOnly Cookie 会话、令牌摘要存储和撤销。
- 用户隔离的多槽位存档与 `save_revision` CAS 覆盖保护。
- Alembic 初始迁移，以及 SQLite/PostgreSQL 配置边界。
- Edge TTS 有界并发、SHA-256 缓存、原子落盘和容量清理。
- 内容校验命令、同域冒烟脚本和四 job GitHub Actions 流水线。
- Vue Router、Pinia 与 PixiJS 电影舞台边界，以及六段式故事路由。
- 1972 西安老巷电影级主场景、青年陈守义人物立绘和类型化美术注册表。
- 1990、2024、2050、2089 四张电影级年代主场景、六张 NPC 立绘与四张代表记忆碎片。
- 按 canonical NPC ID 选择人物立绘的多人物演出契约，支持同场景独立角色肖像。
- 代表碎片的 16:9 电影插镜、响应式构图与图片加载失败自隐藏边界。
- 五年代独立色彩脚本：暖琥珀、煤烟火车绿、冷雨蓝、象牙典礼金与青紫记忆光。
- PixiJS 镜头视差、暖窗光晕、尘埃、暗角和胶片颗粒演出。
- 桌面 2.39:1 与手机竖向双构图、低效设备 30 FPS 和断图 SVG 回退。
- 电影式 HUD、热点、对白、因果选择、碎片、档案、时序、记录与结局演出。

### Changed

- 后端成为内容和游戏规则唯一权威来源；前端改为提交意图并替换完整状态。
- 热区、碎片图和选择 UI 改为消费后端规范化数据。
- 前端 API 统一使用同域 `/api` 和 Credentialed Cookie。
- Nginx 同域代理 `/api/` 与 `/tts/`；容器启动前执行 Alembic。
- 健康检查不再暴露 AI Key 是否存在、Debug 状态等配置能力信息。
- Python 与前端直接依赖更新并锁定；生产依赖审计无高危发现。
- 首页、序章、登录、教程、存档、游戏场和结局统一为东方皮影电影语言。
- 匿名首页仅在存在本地登录标记时校验会话，避免无意义的 401 控制台噪声。
- Service Worker 只在生产注册，开发环境会主动注销旧注册。

### Fixed

- 修复旧前端质量基线中的 TypeScript、ESLint、Prettier、Vitest 和构建失败。
- 修复窄屏对话层遮挡热区、关闭按钮被覆盖和选择按钮不可见。
- 删除前端不存在的碎片 ID、重复状态规则和废弃路由调用。
- 删除两份未使用且无法通过 Python 编译的旧场景脚本。
- TTS 或 DeepSeek 失败不再让主剧情失去可操作性。
- 修复旧 Service Worker 缓存导致本地页面停在“记忆加载中”。
- 修复手机端叙事字幕与因果选项重叠、人物立绘硬切边和档案浮层视觉冲突。
- `prefers-reduced-motion` 下停止 Pixi 舞台和胶片颗粒，并在页面隐藏时暂停舞台。
- 电影主美术加载失败时回退到可操作的旧场景插画，不再白屏。
- 碎片插镜加载失败时保留标题、说明与继续操作，未知碎片继续使用档案图标。

### Security

- 移除前端可读令牌认证和无盐 SHA-256 密码。
- CORS Credential 只允许显式来源，生产 Cookie 默认 Secure。
- 注册冲突和登录失败使用稳定、不泄露账号存在性的错误。
- 存档读取、列出、写入和删除全部限制为当前 Cookie 用户。

### Planned

- 为其余十个后续碎片、四结局高潮和声音主题补齐同等级内容资产。
- 在可控制本机页面的环境完成 390×844、768×1024、1440×900、
  1920×1080 的 1990—2089 人工视觉签收。
- 建立视觉资产授权台账、对象存储/CDN、旧存档兼容和持续内容生产验收流程。

## [0.1.0] - Prototype

- 建立 Vue/FastAPI 原型、基础 SVG 场景、AI 对话和本地存档概念。
