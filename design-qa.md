# 拾忆「记忆推理 + 场景热点」设计验收

- 验收日期：2026-07-28
- 验收范围：1972 年首幕记忆场景、热点交互、证据收集、假说确认、因果选择解锁
- 设计基准：
  - `C:\Users\Administrator\.codex\visualizations\2026\07\26\019f9e41-8aae-7ab0-8203-4d72746caf60\figma-memory-proposal\board-1.png`
  - `C:\Users\Administrator\.codex\visualizations\2026\07\26\019f9e41-8aae-7ab0-8203-4d72746caf60\figma-memory-proposal\board-3.png`
- 实现证据：
  - `.codex-run/qa/implementation-1972-scan-v2.png`
  - `.codex-run/qa/implementation-1972-confirmed-v2.png`
  - `.codex-run/qa/implementation-mobile-390x844.png`
  - `.codex-run/qa/implementation-tablet-1024x768.png`

## 对齐方法

- 桌面实现视口：1280 × 720 CSS px，设备像素比 1.25。
- `board-3.png` 原始尺寸 1440 × 900；对比时等比缩放到 1280 × 800，再取同状态的顶部 720 px。
- `board-1.png` 原始尺寸 1440 × 902；实现的扫描态截图等比缩放并补齐到 1440 × 902。
- 全屏状态对比：`.codex-run/qa/comparison-board3-confirmed-v2.png`。
- 热点样式聚焦对比：`.codex-run/qa/comparison-board1-hotspots-v2.png`。

## 关键表面检查

- 字体：中文叙事使用 `Noto Serif SC`，章节号、状态与英文小标签使用等宽字体；层级和设计基准一致。
- 间距与构图：推理面板保持左上信息区，画面中心和角色/场景主体不被遮挡；第二轮已压缩面板高度和证据卡密度。
- 色彩：沿用项目的墨黑、暖金和低饱和边框令牌，没有引入脱离背景的紫色圆球。
- 热点：全部改为贴合物件位置的角标框和文字标签；未扫描时克制，扫描/悬停/键盘聚焦时增强，收集后退场为安静的档案标记。
- 资产：证据卡使用现有真实碎片图；无占位图、CSS 图形假图或临时素材。
- 文案：任务问题、证据名称、假说陈述和因果选择形成连续的叙事推理链。
- 状态：验证了初始态、扫描态、碎片弹层、两项证据已收集、假说已确认、选择解锁和场景跳转。

## 响应式检查

- 390 × 844：顶部任务、主场景文本和底部锁定提示保持可见，没有横向溢出。
- 1024 × 768：证据面板、场景主体、右侧档案导航和底部因果提示均在视口内，热点仍绑定物件。
- 1280 × 720：完整核心玩法链可见，场景主体留白充足。

## 交互与实现检查

- 真实浏览器完成：扫描全景、收集两件证据、关闭碎片/对白弹层、编织并确认假说、解锁选择、进入 1990 场景。
- 浏览器控制台 warning/error：0。
- 前后端使用服务端确认的证据 ID 和假说状态；刷新或跨场景后不会依赖过期的本地快照。
- 热点使用原生 `button` 和 `:focus-visible`。当前浏览器自动化的按键注入不能把焦点从 `body` 移到页面控件，因此 Enter 键端到端属于工具覆盖缺口；组件语义和焦点视觉已在代码中保留。

## 修正记录

1. P2：首轮推理面板纵向密度偏高，遮挡场景；压缩宽高、内边距、证据卡和按钮高度后重新截图通过。
2. P1：收集后前端仍读取旧的 `sceneFragments.is_collected`，导致证据未即时可用；改用服务端权威 `collected_fragments`，并补充回归测试。
3. P2：原有所有紫色圆球与背景割裂；全局替换为物件角标热点体系，并检查扫描态和已收集态。

final result: passed
